# Agent Notes

Keep HisabAgent an inspectable reconciliation cockpit. Preserve the Planner → Ingestor → Matcher → Critic → Explainer sequence, human exception queue, bilingual explanation, audit export, and eval harness. Run `npm test`, `npm run eval`, and `npm run build` before handoff.
